diff --git a/README.adoc b/README.adoc
index 089eba4976..901e317ae4 100755
--- a/README.adoc
+++ b/README.adoc
@@ -88,4 +88,2 @@ requests. If you want to raise an issue, please follow the recommendations below
-* If possible try to create a test-case or project that replicates the issue. You can
-  submit sample projects as pull-requests against the
-  https://github.com/spring-projects/spring-boot-issues[spring-boot-issues] GitHub
-  project. Use the issue number for the name of your project.
+* If possible try to create a test-case or project that replicates the problem and attach
+  it to the issue.
diff --git a/README.adoc b/README.adoc
index 9cd6d7dc3c..089eba4976 100755
--- a/README.adoc
+++ b/README.adoc
@@ -254,3 +253,0 @@ link:spring-boot-project/spring-boot-cli/samples[spring-boot-cli/samples]. To ru
-Java samples are available in link:spring-boot-samples[spring-boot-samples] and should
-be built with maven and run by invoking `java -jar target/<sample>.jar`.
-
diff --git a/README.adoc b/README.adoc
index 5f98ae5cbf..9cd6d7dc3c 100755
--- a/README.adoc
+++ b/README.adoc
@@ -2,0 +3 @@
+:github: https://github.com/spring-projects/spring-boot
@@ -25,6 +26,5 @@ diverge from the defaults
-The {docs}/htmlsingle/[reference documentation] includes detailed
-{docs}/htmlsingle/#getting-started-installing-spring-boot[installation instructions]
-as well as a comprehensive {docs}/htmlsingle/#getting-started-first-application[``getting
-started``] guide. Documentation is published in {docs}/htmlsingle/[HTML],
-{docs}/pdf/spring-boot-reference.pdf[PDF] and {docs}/epub/spring-boot-reference.epub[EPUB]
-formats.
+The {docs}/html/[reference documentation] includes detailed
+{docs}/html/getting-started.html#getting-started-installing-spring-boot[installation
+instructions] as well as a comprehensive
+{docs}/html/getting-started.html#getting-started-first-application[``getting started``]
+guide.
@@ -61,2 +61,2 @@ Having trouble with Spring Boot? We'd like to help!
-* Check the {docs}/htmlsingle/[reference documentation], especially the
-  {docs}/htmlsingle/#howto[How-to's] -- they provide solutions to the most common
+* Check the {docs}/html/[reference documentation], especially the
+  {docs}/html/howto.html#howto[How-to's] -- they provide solutions to the most common
@@ -67,2 +67,2 @@ Having trouble with Spring Boot? We'd like to help!
-* If you are upgrading, read the https://github.com/spring-projects/spring-boot/wiki[release notes]
-  for upgrade instructions and "new and noteworthy" features.
+* If you are upgrading, read the {github}/wiki[release notes] for upgrade instructions and
+  "new and noteworthy" features.
@@ -72 +72 @@ Having trouble with Spring Boot? We'd like to help!
-* Report bugs with Spring Boot at https://github.com/spring-projects/spring-boot/issues[github.com/spring-projects/spring-boot/issues].
+* Report bugs with Spring Boot at {github}/issues[github.com/spring-projects/spring-boot/issues].
@@ -80,3 +80,3 @@ requests. If you want to raise an issue, please follow the recommendations below
-* Before you log a bug, please https://github.com/spring-projects/spring-boot/search?type=Issues[search the issue tracker]
-  to see if someone has already reported the problem.
-* If the issue doesn't already exist, https://github.com/spring-projects/spring-boot/issues/new[create a new issue].
+* Before you log a bug, please search the {github}/issues[issue tracker] to see if someone
+  has already reported the problem.
+* If the issue doesn't already exist, {github}/issues/new[create a new issue].
@@ -154 +154 @@ Once this is done, you can build the reference documentation with the command be
-TIP: The generated documentation is available from `spring-boot-project/spring-boot-docs/target/contents/reference`
+TIP: The generated documentation is available from `spring-boot-project/spring-boot-docs/target/generated-docs/reference/html`
diff --git a/README.adoc b/README.adoc
index da6de64779..5f98ae5cbf 100755
--- a/README.adoc
+++ b/README.adoc
@@ -1 +1 @@
-= Spring Boot image:https://ci.spring.io/api/v1/teams/spring-boot/pipelines/spring-boot-2.0.x/jobs/build/badge["Build Status", link="https://ci.spring.io/teams/spring-boot/pipelines/spring-boot-2.0.x?groups=Build"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
+= Spring Boot image:https://ci.spring.io/api/v1/teams/spring-boot/pipelines/spring-boot/jobs/build/badge["Build Status", link="https://ci.spring.io/teams/spring-boot/pipelines/spring-boot?groups=Build"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
@@ -49 +49 @@ Here is a quick teaser of a complete Spring Boot application in Java:
-		public static void main(String[] args) throws Exception {
+		public static void main(String[] args) {
@@ -70 +70,2 @@ Having trouble with Spring Boot? We'd like to help!
-  tagged with https://stackoverflow.com/tags/spring-boot[`spring-boot`].
+  tagged with https://stackoverflow.com/tags/spring-boot[`spring-boot`]. You can also chat
+  with the community on https://gitter.im/spring-projects/spring-boot[Gitter].
diff --git a/README.adoc b/README.adoc
index 1578b5c00d..da6de64779 100755
--- a/README.adoc
+++ b/README.adoc
@@ -1,2 +1,2 @@
-= Spring Boot image:https://build.spring.io/plugins/servlet/buildStatusImage/BOOT-PUB["Build Status", link="https://build.spring.io/browse/BOOT-PUB"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
-:docs: http://docs.spring.io/spring-boot/docs/current-SNAPSHOT/reference
+= Spring Boot image:https://ci.spring.io/api/v1/teams/spring-boot/pipelines/spring-boot-2.0.x/jobs/build/badge["Build Status", link="https://ci.spring.io/teams/spring-boot/pipelines/spring-boot-2.0.x?groups=Build"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
+:docs: https://docs.spring.io/spring-boot/docs/current-SNAPSHOT/reference
@@ -65,4 +65,6 @@ Having trouble with Spring Boot? We'd like to help!
-  the http://spring.io[spring.io] web-site for a wealth of reference documentation. If
-  you are just starting out with Spring, try one of the http://spring.io/guides[guides].
-* Ask a question - we monitor http://stackoverflow.com[stackoverflow.com] for questions
-  tagged with http://stackoverflow.com/tags/spring-boot[`spring-boot`].
+  the https://spring.io[spring.io] web-site for a wealth of reference documentation. If
+  you are just starting out with Spring, try one of the https://spring.io/guides[guides].
+* If you are upgrading, read the https://github.com/spring-projects/spring-boot/wiki[release notes]
+  for upgrade instructions and "new and noteworthy" features.
+* Ask a question - we monitor https://stackoverflow.com[stackoverflow.com] for questions
+  tagged with https://stackoverflow.com/tags/spring-boot[`spring-boot`].
@@ -96,2 +98 @@ greatest, Spring Boot can be easily built with the
-https://github.com/takari/maven-wrapper[maven wrapper]. You also need JDK 1.8 (although
-Boot applications can run on Java 1.6).
+https://github.com/takari/maven-wrapper[maven wrapper]. You also need JDK 1.8.
@@ -105 +106 @@ If you want to build with the regular `mvn` command, you will need
-http://maven.apache.org/run-maven/index.html[Maven v3.2.1 or above].
+https://maven.apache.org/run-maven/index.html[Maven v3.5.0 or above].
@@ -117,2 +118 @@ https://support.springsource.com/spring_committer_signup[Contributor's Agreement
-before your first change, however trivial. (Or if you filed such an agreement already for
-another project just mention that in your pull request.)_
+before your first change, however trivial._
@@ -121,0 +122,7 @@ another project just mention that in your pull request.)_
+First of all, make sure you have built the project:
+
+[indent=0]
+----
+	$ ./mvnw clean install
+----
+
@@ -127 +134 @@ available so you need to build that first since it's not generated by default.
-	$ ./mvnw clean install -pl spring-boot-tools/spring-boot-maven-plugin -Pdefault,full
+	$ ./mvnw clean install -pl spring-boot-project/spring-boot-tools/spring-boot-maven-plugin -Pdefault,full
@@ -130,2 +137,3 @@ available so you need to build that first since it's not generated by default.
-The documentation also includes auto-generated information about the starters. To
-allow this information to be collected, the starter projects must be built first:
+The documentation also includes auto-generated information about the starters. You might
+have that in your local repository already (per the first step) but if you want to refresh
+it:
@@ -135 +143 @@ allow this information to be collected, the starter projects must be built first
-	$ ./mvnw clean install -f spring-boot-starters
+	$ ./mvnw clean install -f spring-boot-project/spring-boot-starters
@@ -142 +150 @@ Once this is done, you can build the reference documentation with the command be
-	$ ./mvnw clean prepare-package -pl spring-boot-docs -Pdefault,full
+	$ ./mvnw clean prepare-package -pl spring-boot-project/spring-boot-docs -Pdefault,full
@@ -145 +153 @@ Once this is done, you can build the reference documentation with the command be
-TIP: The generated documentation is available from `spring-boot-docs/target/contents/reference`
+TIP: The generated documentation is available from `spring-boot-project/spring-boot-docs/target/contents/reference`
@@ -160 +168 @@ appropriate Spring `ApplicationContext`
-* Embedded web applications with a choice of container (Tomcat or Jetty for now)
+* Embedded web applications with a choice of container (Tomcat, Jetty or Undertow)
@@ -172 +180 @@ auto-configuration of the Spring context.
-Auto-configuration attempts to deduce which beans a user might need. For example, If
+Auto-configuration attempts to deduce which beans a user might need. For example, if
@@ -197,6 +205,24 @@ can also watch files, automatically recompiling and restarting when they change.
-Spring Boot Actuator provides additional auto-configuration to decorate your application
-with features that make it instantly deployable and supportable in production.  For
-instance if you are writing a JSON web service then it will provide a server, security,
-logging, externalized configuration, management endpoints, an audit abstraction, and
-more. If you want to switch off the built in features, or extend or replace them, it
-makes that really easy as well.
+Actuator endpoints let you monitor and interact with your application.
+Spring Boot Actuator provides the infrastructure required for actuator endpoints. It contains
+annotation support for actuator endpoints. Out of the box, this module provides a number of endpoints
+including the `HealthEndpoint`, `EnvironmentEndpoint`, `BeansEndpoint` and many more.
+
+
+
+=== spring-boot-actuator-autoconfigure
+This provides auto-configuration for actuator endpoints based on the content of the classpath and a set of properties.
+For instance, if Micrometer is on the classpath, it will auto-configure the `MetricsEndpoint`.
+It contains configuration to expose endpoints over HTTP or JMX.
+Just like Spring Boot AutoConfigure, this will back away as the user starts to define their own beans.
+
+
+
+=== spring-boot-test
+This module contains core items and annotations that can be helpful when testing your application.
+
+
+
+=== spring-boot-test-autoconfigure
+Like other Spring Boot auto-configuration modules, spring-boot-test-autoconfigure, provides auto-configuration
+for tests based on the classpath. It includes a number of annotations that can be used to automatically
+configure a slice of your application that needs to be tested.
@@ -210,2 +236,9 @@ that can be launched using `java -jar`. Generally you will not need to use
-link:spring-boot-tools/spring-boot-gradle-plugin[Gradle] or
-link:spring-boot-tools/spring-boot-maven-plugin[Maven] plugin.
+link:spring-boot-project/spring-boot-tools/spring-boot-gradle-plugin[Gradle] or
+link:spring-boot-project/spring-boot-tools/spring-boot-maven-plugin[Maven] plugin.
+
+
+
+=== spring-boot-devtools
+The spring-boot-devtools module provides additional development-time features such as automatic restarts,
+for a smoother application development experience. Developer tools are automatically disabled when
+running a fully packaged application.
@@ -217 +250 @@ Groovy samples for use with the command line application are available in
-link:spring-boot-cli/samples[spring-boot-cli/samples]. To run the CLI samples type
+link:spring-boot-project/spring-boot-cli/samples[spring-boot-cli/samples]. To run the CLI samples type
@@ -226 +259 @@ be built with maven and run by invoking `java -jar target/<sample>.jar`.
-The http://spring.io/[spring.io] site contains several guides that show how to use Spring
+The https://spring.io/[spring.io] site contains several guides that show how to use Spring
@@ -229 +262 @@ Boot step-by-step:
-* http://spring.io/guides/gs/spring-boot/[Building an Application with Spring Boot] is a
+* https://spring.io/guides/gs/spring-boot/[Building an Application with Spring Boot] is a
@@ -232 +265 @@ Boot step-by-step:
-* http://spring.io/guides/gs/actuator-service/[Building a RESTful Web Service with Spring
+* https://spring.io/guides/gs/actuator-service/[Building a RESTful Web Service with Spring
@@ -235 +268 @@ Boot step-by-step:
-* http://spring.io/guides/gs/convert-jar-to-war/[Converting a Spring Boot JAR Application
+* https://spring.io/guides/gs/convert-jar-to-war/[Converting a Spring Boot JAR Application
diff --git a/README.adoc b/README.adoc
index ff8a50ae56..1578b5c00d 100755
--- a/README.adoc
+++ b/README.adoc
@@ -94 +94 @@ You don't need to build from source to use Spring Boot (binaries in
-http://repo.spring.io[repo.spring.io]), but if you want to try out the latest and
+https://repo.spring.io[repo.spring.io]), but if you want to try out the latest and
diff --git a/README.adoc b/README.adoc
index b8aa02095d..ff8a50ae56 100755
--- a/README.adoc
+++ b/README.adoc
@@ -41 +41 @@ Here is a quick teaser of a complete Spring Boot application in Java:
-	@EnableAutoConfiguration
+	@SpringBootApplication
@@ -67 +67 @@ Having trouble with Spring Boot? We'd like to help!
-* Ask a questions - we monitor http://stackoverflow.com[stackoverflow.com] for questions
+* Ask a question - we monitor http://stackoverflow.com[stackoverflow.com] for questions
@@ -83 +83 @@ requests. If you want to raise an issue, please follow the recommendations below
-* If you need to paste code, or include a stack trace use Markdown ++$$```$$++ escapes
+* If you need to paste code, or include a stack trace use Markdown +++```+++ escapes
@@ -105 +105 @@ If you want to build with the regular `mvn` command, you will need
-http://maven.apache.org/run-maven/index.html[Maven v3.0.5 or above].
+http://maven.apache.org/run-maven/index.html[Maven v3.2.1 or above].
@@ -117 +117 @@ https://support.springsource.com/spring_committer_signup[Contributor's Agreement
-before your first change however trivial. (Or if you filed such an agreement already for
+before your first change, however trivial. (Or if you filed such an agreement already for
@@ -122 +122 @@ another project just mention that in your pull request.)_
-The reference documentation requires the documentation of the maven plugin to be
+The reference documentation requires the documentation of the Maven plugin to be
@@ -129,0 +130,8 @@ available so you need to build that first since it's not generated by default.
+The documentation also includes auto-generated information about the starters. To
+allow this information to be collected, the starter projects must be built first:
+
+[indent=0]
+----
+	$ ./mvnw clean install -f spring-boot-starters
+----
+
@@ -134 +142 @@ Once this is done, you can build the reference documentation with the command be
-	$ ./mvnw clean install -pl spring-boot-docs -Pdefault,full
+	$ ./mvnw clean prepare-package -pl spring-boot-docs -Pdefault,full
diff --git a/README.adoc b/README.adoc
index 0ed239235d..b8aa02095d 100755
--- a/README.adoc
+++ b/README.adoc
@@ -1,3 +1,2 @@
-= Spring Boot image:https://ci.spring.io/api/v1/teams/spring-boot/pipelines/spring-boot/jobs/build/badge["Build Status", link="https://ci.spring.io/teams/spring-boot/pipelines/spring-boot?groups=Build"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
-:docs: https://docs.spring.io/spring-boot/docs/current-SNAPSHOT/reference
-:github: https://github.com/spring-projects/spring-boot
+= Spring Boot image:https://build.spring.io/plugins/servlet/buildStatusImage/BOOT-PUB["Build Status", link="https://build.spring.io/browse/BOOT-PUB"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
+:docs: http://docs.spring.io/spring-boot/docs/current-SNAPSHOT/reference
@@ -26,5 +25,6 @@ diverge from the defaults
-The {docs}/html/[reference documentation] includes detailed
-{docs}/html/getting-started.html#getting-started-installing-spring-boot[installation
-instructions] as well as a comprehensive
-{docs}/html/getting-started.html#getting-started-first-application[``getting started``]
-guide.
+The {docs}/htmlsingle/[reference documentation] includes detailed
+{docs}/htmlsingle/#getting-started-installing-spring-boot[installation instructions]
+as well as a comprehensive {docs}/htmlsingle/#getting-started-first-application[``getting
+started``] guide. Documentation is published in {docs}/htmlsingle/[HTML],
+{docs}/pdf/spring-boot-reference.pdf[PDF] and {docs}/epub/spring-boot-reference.epub[EPUB]
+formats.
@@ -41 +41 @@ Here is a quick teaser of a complete Spring Boot application in Java:
-	@SpringBootApplication
+	@EnableAutoConfiguration
@@ -49 +49 @@ Here is a quick teaser of a complete Spring Boot application in Java:
-		public static void main(String[] args) {
+		public static void main(String[] args) throws Exception {
@@ -61,2 +61,2 @@ Having trouble with Spring Boot? We'd like to help!
-* Check the {docs}/html/[reference documentation], especially the
-  {docs}/html/howto.html#howto[How-to's] -- they provide solutions to the most common
+* Check the {docs}/htmlsingle/[reference documentation], especially the
+  {docs}/htmlsingle/#howto[How-to's] -- they provide solutions to the most common
@@ -65,8 +65,5 @@ Having trouble with Spring Boot? We'd like to help!
-  the https://spring.io[spring.io] web-site for a wealth of reference documentation. If
-  you are just starting out with Spring, try one of the https://spring.io/guides[guides].
-* If you are upgrading, read the {github}/wiki[release notes] for upgrade instructions and
-  "new and noteworthy" features.
-* Ask a question - we monitor https://stackoverflow.com[stackoverflow.com] for questions
-  tagged with https://stackoverflow.com/tags/spring-boot[`spring-boot`]. You can also chat
-  with the community on https://gitter.im/spring-projects/spring-boot[Gitter].
-* Report bugs with Spring Boot at {github}/issues[github.com/spring-projects/spring-boot/issues].
+  the http://spring.io[spring.io] web-site for a wealth of reference documentation. If
+  you are just starting out with Spring, try one of the http://spring.io/guides[guides].
+* Ask a questions - we monitor http://stackoverflow.com[stackoverflow.com] for questions
+  tagged with http://stackoverflow.com/tags/spring-boot[`spring-boot`].
+* Report bugs with Spring Boot at https://github.com/spring-projects/spring-boot/issues[github.com/spring-projects/spring-boot/issues].
@@ -80,3 +77,3 @@ requests. If you want to raise an issue, please follow the recommendations below
-* Before you log a bug, please search the {github}/issues[issue tracker] to see if someone
-  has already reported the problem.
-* If the issue doesn't already exist, {github}/issues/new[create a new issue].
+* Before you log a bug, please https://github.com/spring-projects/spring-boot/search?type=Issues[search the issue tracker]
+  to see if someone has already reported the problem.
+* If the issue doesn't already exist, https://github.com/spring-projects/spring-boot/issues/new[create a new issue].
@@ -86 +83 @@ requests. If you want to raise an issue, please follow the recommendations below
-* If you need to paste code, or include a stack trace use Markdown +++```+++ escapes
+* If you need to paste code, or include a stack trace use Markdown ++$$```$$++ escapes
@@ -97 +94 @@ You don't need to build from source to use Spring Boot (binaries in
-https://repo.spring.io[repo.spring.io]), but if you want to try out the latest and
+http://repo.spring.io[repo.spring.io]), but if you want to try out the latest and
@@ -99 +96,2 @@ greatest, Spring Boot can be easily built with the
-https://github.com/takari/maven-wrapper[maven wrapper]. You also need JDK 1.8.
+https://github.com/takari/maven-wrapper[maven wrapper]. You also need JDK 1.8 (although
+Boot applications can run on Java 1.6).
@@ -107 +105 @@ If you want to build with the regular `mvn` command, you will need
-https://maven.apache.org/run-maven/index.html[Maven v3.5.0 or above].
+http://maven.apache.org/run-maven/index.html[Maven v3.0.5 or above].
@@ -119 +117,2 @@ https://support.springsource.com/spring_committer_signup[Contributor's Agreement
-before your first change, however trivial._
+before your first change however trivial. (Or if you filed such an agreement already for
+another project just mention that in your pull request.)_
@@ -123,8 +122 @@ before your first change, however trivial._
-First of all, make sure you have built the project:
-
-[indent=0]
-----
-	$ ./mvnw clean install
-----
-
-The reference documentation requires the documentation of the Maven plugin to be
+The reference documentation requires the documentation of the maven plugin to be
@@ -135,10 +127 @@ available so you need to build that first since it's not generated by default.
-	$ ./mvnw clean install -pl spring-boot-project/spring-boot-tools/spring-boot-maven-plugin -Pdefault,full
-----
-
-The documentation also includes auto-generated information about the starters. You might
-have that in your local repository already (per the first step) but if you want to refresh
-it:
-
-[indent=0]
-----
-	$ ./mvnw clean install -f spring-boot-project/spring-boot-starters
+	$ ./mvnw clean install -pl spring-boot-tools/spring-boot-maven-plugin -Pdefault,full
@@ -151 +134 @@ Once this is done, you can build the reference documentation with the command be
-	$ ./mvnw clean prepare-package -pl spring-boot-project/spring-boot-docs -Pdefault,full
+	$ ./mvnw clean install -pl spring-boot-docs -Pdefault,full
@@ -154 +137 @@ Once this is done, you can build the reference documentation with the command be
-TIP: The generated documentation is available from `spring-boot-project/spring-boot-docs/target/generated-docs/reference/html`
+TIP: The generated documentation is available from `spring-boot-docs/target/contents/reference`
@@ -169 +152 @@ appropriate Spring `ApplicationContext`
-* Embedded web applications with a choice of container (Tomcat, Jetty or Undertow)
+* Embedded web applications with a choice of container (Tomcat or Jetty for now)
@@ -181 +164 @@ auto-configuration of the Spring context.
-Auto-configuration attempts to deduce which beans a user might need. For example, if
+Auto-configuration attempts to deduce which beans a user might need. For example, If
@@ -206,24 +189,6 @@ can also watch files, automatically recompiling and restarting when they change.
-Actuator endpoints let you monitor and interact with your application.
-Spring Boot Actuator provides the infrastructure required for actuator endpoints. It contains
-annotation support for actuator endpoints. Out of the box, this module provides a number of endpoints
-including the `HealthEndpoint`, `EnvironmentEndpoint`, `BeansEndpoint` and many more.
-
-
-
-=== spring-boot-actuator-autoconfigure
-This provides auto-configuration for actuator endpoints based on the content of the classpath and a set of properties.
-For instance, if Micrometer is on the classpath, it will auto-configure the `MetricsEndpoint`.
-It contains configuration to expose endpoints over HTTP or JMX.
-Just like Spring Boot AutoConfigure, this will back away as the user starts to define their own beans.
-
-
-
-=== spring-boot-test
-This module contains core items and annotations that can be helpful when testing your application.
-
-
-
-=== spring-boot-test-autoconfigure
-Like other Spring Boot auto-configuration modules, spring-boot-test-autoconfigure, provides auto-configuration
-for tests based on the classpath. It includes a number of annotations that can be used to automatically
-configure a slice of your application that needs to be tested.
+Spring Boot Actuator provides additional auto-configuration to decorate your application
+with features that make it instantly deployable and supportable in production.  For
+instance if you are writing a JSON web service then it will provide a server, security,
+logging, externalized configuration, management endpoints, an audit abstraction, and
+more. If you want to switch off the built in features, or extend or replace them, it
+makes that really easy as well.
@@ -237,9 +202,2 @@ that can be launched using `java -jar`. Generally you will not need to use
-link:spring-boot-project/spring-boot-tools/spring-boot-gradle-plugin[Gradle] or
-link:spring-boot-project/spring-boot-tools/spring-boot-maven-plugin[Maven] plugin.
-
-
-
-=== spring-boot-devtools
-The spring-boot-devtools module provides additional development-time features such as automatic restarts,
-for a smoother application development experience. Developer tools are automatically disabled when
-running a fully packaged application.
+link:spring-boot-tools/spring-boot-gradle-plugin[Gradle] or
+link:spring-boot-tools/spring-boot-maven-plugin[Maven] plugin.
@@ -251 +209 @@ Groovy samples for use with the command line application are available in
-link:spring-boot-project/spring-boot-cli/samples[spring-boot-cli/samples]. To run the CLI samples type
+link:spring-boot-cli/samples[spring-boot-cli/samples]. To run the CLI samples type
@@ -260 +218 @@ be built with maven and run by invoking `java -jar target/<sample>.jar`.
-The https://spring.io/[spring.io] site contains several guides that show how to use Spring
+The http://spring.io/[spring.io] site contains several guides that show how to use Spring
@@ -263 +221 @@ Boot step-by-step:
-* https://spring.io/guides/gs/spring-boot/[Building an Application with Spring Boot] is a
+* http://spring.io/guides/gs/spring-boot/[Building an Application with Spring Boot] is a
@@ -266 +224 @@ Boot step-by-step:
-* https://spring.io/guides/gs/actuator-service/[Building a RESTful Web Service with Spring
+* http://spring.io/guides/gs/actuator-service/[Building a RESTful Web Service with Spring
@@ -269 +227 @@ Boot step-by-step:
-* https://spring.io/guides/gs/convert-jar-to-war/[Converting a Spring Boot JAR Application
+* http://spring.io/guides/gs/convert-jar-to-war/[Converting a Spring Boot JAR Application
@@ -276 +234 @@ Spring Boot is Open Source software released under the
-http://www.apache.org/licenses/LICENSE-2.0.html[Apache 2.0 license].
+https://www.apache.org/licenses/LICENSE-2.0.html[Apache 2.0 license].
diff --git a/README.adoc b/README.adoc
index 69ec98a4f0..0ed239235d 100755
--- a/README.adoc
+++ b/README.adoc
@@ -154 +154 @@ Once this is done, you can build the reference documentation with the command be
-TIP: The generated documentation is available from `spring-boot-project/spring-boot-docs/target/contents/reference`
+TIP: The generated documentation is available from `spring-boot-project/spring-boot-docs/target/generated-docs/reference/html`
diff --git a/README.adoc b/README.adoc
index 1491be19eb..69ec98a4f0 100755
--- a/README.adoc
+++ b/README.adoc
@@ -2,0 +3 @@
+:github: https://github.com/spring-projects/spring-boot
@@ -25,6 +26,5 @@ diverge from the defaults
-The {docs}/htmlsingle/[reference documentation] includes detailed
-{docs}/htmlsingle/#getting-started-installing-spring-boot[installation instructions]
-as well as a comprehensive {docs}/htmlsingle/#getting-started-first-application[``getting
-started``] guide. Documentation is published in {docs}/htmlsingle/[HTML],
-{docs}/pdf/spring-boot-reference.pdf[PDF] and {docs}/epub/spring-boot-reference.epub[EPUB]
-formats.
+The {docs}/html/[reference documentation] includes detailed
+{docs}/html/getting-started.html#getting-started-installing-spring-boot[installation
+instructions] as well as a comprehensive
+{docs}/html/getting-started.html#getting-started-first-application[``getting started``]
+guide.
@@ -61,2 +61,2 @@ Having trouble with Spring Boot? We'd like to help!
-* Check the {docs}/htmlsingle/[reference documentation], especially the
-  {docs}/htmlsingle/#howto[How-to's] -- they provide solutions to the most common
+* Check the {docs}/html/[reference documentation], especially the
+  {docs}/html/howto.html#howto[How-to's] -- they provide solutions to the most common
@@ -67,2 +67,2 @@ Having trouble with Spring Boot? We'd like to help!
-* If you are upgrading, read the https://github.com/spring-projects/spring-boot/wiki[release notes]
-  for upgrade instructions and "new and noteworthy" features.
+* If you are upgrading, read the {github}/wiki[release notes] for upgrade instructions and
+  "new and noteworthy" features.
@@ -72 +72 @@ Having trouble with Spring Boot? We'd like to help!
-* Report bugs with Spring Boot at https://github.com/spring-projects/spring-boot/issues[github.com/spring-projects/spring-boot/issues].
+* Report bugs with Spring Boot at {github}/issues[github.com/spring-projects/spring-boot/issues].
@@ -80,3 +80,3 @@ requests. If you want to raise an issue, please follow the recommendations below
-* Before you log a bug, please https://github.com/spring-projects/spring-boot/search?type=Issues[search the issue tracker]
-  to see if someone has already reported the problem.
-* If the issue doesn't already exist, https://github.com/spring-projects/spring-boot/issues/new[create a new issue].
+* Before you log a bug, please search the {github}/issues[issue tracker] to see if someone
+  has already reported the problem.
+* If the issue doesn't already exist, {github}/issues/new[create a new issue].
diff --git a/README.adoc b/README.adoc
index c48288d293..1491be19eb 100755
--- a/README.adoc
+++ b/README.adoc
@@ -3 +2,0 @@
-:github: https://github.com/spring-projects/spring-boot
@@ -26,5 +25,6 @@ diverge from the defaults
-The {docs}/html/[reference documentation] includes detailed
-{docs}/html/getting-started.html#getting-started-installing-spring-boot[installation
-instructions] as well as a comprehensive
-{docs}/html/getting-started.html#getting-started-first-application[``getting started``]
-guide.
+The {docs}/htmlsingle/[reference documentation] includes detailed
+{docs}/htmlsingle/#getting-started-installing-spring-boot[installation instructions]
+as well as a comprehensive {docs}/htmlsingle/#getting-started-first-application[``getting
+started``] guide. Documentation is published in {docs}/htmlsingle/[HTML],
+{docs}/pdf/spring-boot-reference.pdf[PDF] and {docs}/epub/spring-boot-reference.epub[EPUB]
+formats.
@@ -49 +49 @@ Here is a quick teaser of a complete Spring Boot application in Java:
-		public static void main(String[] args) throws Exception {
+		public static void main(String[] args) {
@@ -61,2 +61,2 @@ Having trouble with Spring Boot? We'd like to help!
-* Check the {docs}/html/[reference documentation], especially the
-  {docs}/html/howto.html#howto[How-to's] -- they provide solutions to the most common
+* Check the {docs}/htmlsingle/[reference documentation], especially the
+  {docs}/htmlsingle/#howto[How-to's] -- they provide solutions to the most common
@@ -67,2 +67,2 @@ Having trouble with Spring Boot? We'd like to help!
-* If you are upgrading, read the {github}/wiki[release notes] for upgrade instructions and
-  "new and noteworthy" features.
+* If you are upgrading, read the https://github.com/spring-projects/spring-boot/wiki[release notes]
+  for upgrade instructions and "new and noteworthy" features.
@@ -72 +72 @@ Having trouble with Spring Boot? We'd like to help!
-* Report bugs with Spring Boot at {github}/issues[github.com/spring-projects/spring-boot/issues].
+* Report bugs with Spring Boot at https://github.com/spring-projects/spring-boot/issues[github.com/spring-projects/spring-boot/issues].
@@ -80,3 +80,3 @@ requests. If you want to raise an issue, please follow the recommendations below
-* Before you log a bug, please search the {github}/issues[issue tracker] to see if someone
-  has already reported the problem.
-* If the issue doesn't already exist, {github}/issues/new[create a new issue].
+* Before you log a bug, please https://github.com/spring-projects/spring-boot/search?type=Issues[search the issue tracker]
+  to see if someone has already reported the problem.
+* If the issue doesn't already exist, https://github.com/spring-projects/spring-boot/issues/new[create a new issue].
diff --git a/README.adoc b/README.adoc
index d4a3960968..c48288d293 100755
--- a/README.adoc
+++ b/README.adoc
@@ -2,0 +3 @@
+:github: https://github.com/spring-projects/spring-boot
@@ -25,6 +26,5 @@ diverge from the defaults
-The {docs}/htmlsingle/[reference documentation] includes detailed
-{docs}/htmlsingle/#getting-started-installing-spring-boot[installation instructions]
-as well as a comprehensive {docs}/htmlsingle/#getting-started-first-application[``getting
-started``] guide. Documentation is published in {docs}/htmlsingle/[HTML],
-{docs}/pdf/spring-boot-reference.pdf[PDF] and {docs}/epub/spring-boot-reference.epub[EPUB]
-formats.
+The {docs}/html/[reference documentation] includes detailed
+{docs}/html/getting-started.html#getting-started-installing-spring-boot[installation
+instructions] as well as a comprehensive
+{docs}/html/getting-started.html#getting-started-first-application[``getting started``]
+guide.
@@ -61,2 +61,2 @@ Having trouble with Spring Boot? We'd like to help!
-* Check the {docs}/htmlsingle/[reference documentation], especially the
-  {docs}/htmlsingle/#howto[How-to's] -- they provide solutions to the most common
+* Check the {docs}/html/[reference documentation], especially the
+  {docs}/html/howto.html#howto[How-to's] -- they provide solutions to the most common
@@ -67,2 +67,2 @@ Having trouble with Spring Boot? We'd like to help!
-* If you are upgrading, read the https://github.com/spring-projects/spring-boot/wiki[release notes]
-  for upgrade instructions and "new and noteworthy" features.
+* If you are upgrading, read the {github}/wiki[release notes] for upgrade instructions and
+  "new and noteworthy" features.
@@ -72 +72 @@ Having trouble with Spring Boot? We'd like to help!
-* Report bugs with Spring Boot at https://github.com/spring-projects/spring-boot/issues[github.com/spring-projects/spring-boot/issues].
+* Report bugs with Spring Boot at {github}/issues[github.com/spring-projects/spring-boot/issues].
@@ -80,3 +80,3 @@ requests. If you want to raise an issue, please follow the recommendations below
-* Before you log a bug, please https://github.com/spring-projects/spring-boot/search?type=Issues[search the issue tracker]
-  to see if someone has already reported the problem.
-* If the issue doesn't already exist, https://github.com/spring-projects/spring-boot/issues/new[create a new issue].
+* Before you log a bug, please search the {github}/issues[issue tracker] to see if someone
+  has already reported the problem.
+* If the issue doesn't already exist, {github}/issues/new[create a new issue].
diff --git a/README.adoc b/README.adoc
index 7b016d158c..d4a3960968 100755
--- a/README.adoc
+++ b/README.adoc
@@ -1 +1 @@
-= Spring Boot image:https://ci.spring.io/api/v1/teams/spring-boot/pipelines/spring-boot-2.0.x/jobs/build/badge["Build Status", link="https://ci.spring.io/teams/spring-boot/pipelines/spring-boot-2.0.x?groups=Build"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
+= Spring Boot image:https://ci.spring.io/api/v1/teams/spring-boot/pipelines/spring-boot/jobs/build/badge["Build Status", link="https://ci.spring.io/teams/spring-boot/pipelines/spring-boot?groups=Build"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
@@ -70 +70,2 @@ Having trouble with Spring Boot? We'd like to help!
-  tagged with https://stackoverflow.com/tags/spring-boot[`spring-boot`].
+  tagged with https://stackoverflow.com/tags/spring-boot[`spring-boot`]. You can also chat
+  with the community on https://gitter.im/spring-projects/spring-boot[Gitter].
diff --git a/README.adoc b/README.adoc
index e31172aea8..7b016d158c 100755
--- a/README.adoc
+++ b/README.adoc
@@ -1 +1 @@
-= Spring Boot image:https://ci.spring.io/api/v1/teams/spring-boot/pipelines/spring-boot/jobs/build/badge["Build Status", link="https://ci.spring.io/teams/spring-boot/pipelines/spring-boot?groups=Build"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
+= Spring Boot image:https://ci.spring.io/api/v1/teams/spring-boot/pipelines/spring-boot-2.0.x/jobs/build/badge["Build Status", link="https://ci.spring.io/teams/spring-boot/pipelines/spring-boot-2.0.x?groups=Build"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
@@ -70,2 +70 @@ Having trouble with Spring Boot? We'd like to help!
-  tagged with https://stackoverflow.com/tags/spring-boot[`spring-boot`]. You can also chat
-  with the community on https://gitter.im/spring-projects/spring-boot[Gitter].
+  tagged with https://stackoverflow.com/tags/spring-boot[`spring-boot`].
@@ -209 +208 @@ annotation support for actuator endpoints. Out of the box, this module provides
-including the `HealthEndpoint`, `EnvironmentEndpoint`, `BeansEndpoints` and many more.
+including the `HealthEndpoint`, `EnvironmentEndpoint`, `BeansEndpoint` and many more.
diff --git a/README.adoc b/README.adoc
index 36381005a1..e31172aea8 100755
--- a/README.adoc
+++ b/README.adoc
@@ -1 +1 @@
-= Spring Boot image:https://ci.spring.io/api/v1/teams/spring-boot/pipelines/spring-boot-2.0.x/jobs/build/badge["Build Status", link="https://ci.spring.io/teams/spring-boot/pipelines/spring-boot-2.0.x?groups=Build"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
+= Spring Boot image:https://ci.spring.io/api/v1/teams/spring-boot/pipelines/spring-boot/jobs/build/badge["Build Status", link="https://ci.spring.io/teams/spring-boot/pipelines/spring-boot?groups=Build"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
diff --git a/README.adoc b/README.adoc
index 37836f50f0..36381005a1 100755
--- a/README.adoc
+++ b/README.adoc
@@ -70 +70,2 @@ Having trouble with Spring Boot? We'd like to help!
-  tagged with https://stackoverflow.com/tags/spring-boot[`spring-boot`].
+  tagged with https://stackoverflow.com/tags/spring-boot[`spring-boot`]. You can also chat
+  with the community on https://gitter.im/spring-projects/spring-boot[Gitter].
diff --git a/README.adoc b/README.adoc
index de39eb2556..37836f50f0 100755
--- a/README.adoc
+++ b/README.adoc
@@ -70,2 +70 @@ Having trouble with Spring Boot? We'd like to help!
-  tagged with https://stackoverflow.com/tags/spring-boot[`spring-boot`]. You can also chat
-  with the community on https://gitter.im/spring-projects/spring-boot[Gitter].
+  tagged with https://stackoverflow.com/tags/spring-boot[`spring-boot`].
@@ -227 +226 @@ This module contains core items and annotations that can be helpful when testing
-Like other Spring Boot Auto-Configuration modules, spring-boot-test-autoconfigure, provides auto-configuration
+Like other Spring Boot auto-configuration modules, spring-boot-test-autoconfigure, provides auto-configuration
diff --git a/README.adoc b/README.adoc
index 5c824aad0a..de39eb2556 100755
--- a/README.adoc
+++ b/README.adoc
@@ -70 +70,2 @@ Having trouble with Spring Boot? We'd like to help!
-  tagged with https://stackoverflow.com/tags/spring-boot[`spring-boot`].
+  tagged with https://stackoverflow.com/tags/spring-boot[`spring-boot`]. You can also chat
+  with the community on https://gitter.im/spring-projects/spring-boot[Gitter].
diff --git a/README.adoc b/README.adoc
index 22e372b6b3..5c824aad0a 100755
--- a/README.adoc
+++ b/README.adoc
@@ -106 +106 @@ If you want to build with the regular `mvn` command, you will need
-https://maven.apache.org/run-maven/index.html[Maven v3.2.1 or above].
+https://maven.apache.org/run-maven/index.html[Maven v3.5.0 or above].
diff --git a/README.adoc b/README.adoc
index e803cb0697..22e372b6b3 100755
--- a/README.adoc
+++ b/README.adoc
@@ -1 +1 @@
-= Spring Boot image:https://ci.spring.io/api/v1/teams/spring-boot/pipelines/spring-boot/jobs/build/badge["Build Status", link="https://ci.spring.io/teams/spring-boot/pipelines/spring-boot?groups=Build"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
+= Spring Boot image:https://ci.spring.io/api/v1/teams/spring-boot/pipelines/spring-boot-2.0.x/jobs/build/badge["Build Status", link="https://ci.spring.io/teams/spring-boot/pipelines/spring-boot-2.0.x?groups=Build"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
diff --git a/README.adoc b/README.adoc
index 98f4ac6850..e803cb0697 100755
--- a/README.adoc
+++ b/README.adoc
@@ -242 +242 @@ link:spring-boot-project/spring-boot-tools/spring-boot-maven-plugin[Maven] plugi
-The spring-boot-devtools module provides additional development-time features such as automatic retarts,
+The spring-boot-devtools module provides additional development-time features such as automatic restarts,
diff --git a/README.adoc b/README.adoc
index 8af2c51f95..98f4ac6850 100755
--- a/README.adoc
+++ b/README.adoc
@@ -205,6 +205,24 @@ can also watch files, automatically recompiling and restarting when they change.
-Spring Boot Actuator provides additional auto-configuration to decorate your application
-with features that make it instantly deployable and supportable in production. For
-instance, if you are writing a JSON web service then it will provide a server, security,
-logging, externalized configuration, management endpoints, an audit abstraction, and
-more. If you want to switch off the built-in features, or extend or replace them, it
-makes that really easy as well.
+Actuator endpoints let you monitor and interact with your application.
+Spring Boot Actuator provides the infrastructure required for actuator endpoints. It contains
+annotation support for actuator endpoints. Out of the box, this module provides a number of endpoints
+including the `HealthEndpoint`, `EnvironmentEndpoint`, `BeansEndpoints` and many more.
+
+
+
+=== spring-boot-actuator-autoconfigure
+This provides auto-configuration for actuator endpoints based on the content of the classpath and a set of properties.
+For instance, if Micrometer is on the classpath, it will auto-configure the `MetricsEndpoint`.
+It contains configuration to expose endpoints over HTTP or JMX.
+Just like Spring Boot AutoConfigure, this will back away as the user starts to define their own beans.
+
+
+
+=== spring-boot-test
+This module contains core items and annotations that can be helpful when testing your application.
+
+
+
+=== spring-boot-test-autoconfigure
+Like other Spring Boot Auto-Configuration modules, spring-boot-test-autoconfigure, provides auto-configuration
+for tests based on the classpath. It includes a number of annotations that can be used to automatically
+configure a slice of your application that needs to be tested.
@@ -222,0 +241,7 @@ link:spring-boot-project/spring-boot-tools/spring-boot-maven-plugin[Maven] plugi
+=== spring-boot-devtools
+The spring-boot-devtools module provides additional development-time features such as automatic retarts,
+for a smoother application development experience. Developer tools are automatically disabled when
+running a fully packaged application.
+
+
+
diff --git a/README.adoc b/README.adoc
index 99f9cc7320..8af2c51f95 100755
--- a/README.adoc
+++ b/README.adoc
@@ -1,2 +1,2 @@
-= Spring Boot image:https://build.spring.io/plugins/servlet/buildStatusImage/BOOT-PUB["Build Status", link="https://build.spring.io/browse/BOOT-PUB"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
-:docs: http://docs.spring.io/spring-boot/docs/current-SNAPSHOT/reference
+= Spring Boot image:https://ci.spring.io/api/v1/teams/spring-boot/pipelines/spring-boot/jobs/build/badge["Build Status", link="https://ci.spring.io/teams/spring-boot/pipelines/spring-boot?groups=Build"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
+:docs: https://docs.spring.io/spring-boot/docs/current-SNAPSHOT/reference
@@ -65,4 +65,6 @@ Having trouble with Spring Boot? We'd like to help!
-  the http://spring.io[spring.io] web-site for a wealth of reference documentation. If
-  you are just starting out with Spring, try one of the http://spring.io/guides[guides].
-* Ask a question - we monitor http://stackoverflow.com[stackoverflow.com] for questions
-  tagged with http://stackoverflow.com/tags/spring-boot[`spring-boot`].
+  the https://spring.io[spring.io] web-site for a wealth of reference documentation. If
+  you are just starting out with Spring, try one of the https://spring.io/guides[guides].
+* If you are upgrading, read the https://github.com/spring-projects/spring-boot/wiki[release notes]
+  for upgrade instructions and "new and noteworthy" features.
+* Ask a question - we monitor https://stackoverflow.com[stackoverflow.com] for questions
+  tagged with https://stackoverflow.com/tags/spring-boot[`spring-boot`].
@@ -96,2 +98 @@ greatest, Spring Boot can be easily built with the
-https://github.com/takari/maven-wrapper[maven wrapper]. You also need JDK 1.8 (although
-Boot applications can run on Java 1.6).
+https://github.com/takari/maven-wrapper[maven wrapper]. You also need JDK 1.8.
@@ -105 +106 @@ If you want to build with the regular `mvn` command, you will need
-http://maven.apache.org/run-maven/index.html[Maven v3.2.1 or above].
+https://maven.apache.org/run-maven/index.html[Maven v3.2.1 or above].
@@ -117,2 +118 @@ https://support.springsource.com/spring_committer_signup[Contributor's Agreement
-before your first change, however trivial. (Or if you filed such an agreement already for
-another project just mention that in your pull request.)_
+before your first change, however trivial._
@@ -121,0 +122,7 @@ another project just mention that in your pull request.)_
+First of all, make sure you have built the project:
+
+[indent=0]
+----
+	$ ./mvnw clean install
+----
+
@@ -127 +134 @@ available so you need to build that first since it's not generated by default.
-	$ ./mvnw clean install -pl spring-boot-tools/spring-boot-maven-plugin -Pdefault,full
+	$ ./mvnw clean install -pl spring-boot-project/spring-boot-tools/spring-boot-maven-plugin -Pdefault,full
@@ -130,2 +137,3 @@ available so you need to build that first since it's not generated by default.
-The documentation also includes auto-generated information about the starters. To
-allow this information to be collected, the starter projects must be built first:
+The documentation also includes auto-generated information about the starters. You might
+have that in your local repository already (per the first step) but if you want to refresh
+it:
@@ -135 +143 @@ allow this information to be collected, the starter projects must be built first
-	$ ./mvnw clean install -f spring-boot-starters
+	$ ./mvnw clean install -f spring-boot-project/spring-boot-starters
@@ -142 +150 @@ Once this is done, you can build the reference documentation with the command be
-	$ ./mvnw clean prepare-package -pl spring-boot-docs -Pdefault,full
+	$ ./mvnw clean prepare-package -pl spring-boot-project/spring-boot-docs -Pdefault,full
@@ -145 +153 @@ Once this is done, you can build the reference documentation with the command be
-TIP: The generated documentation is available from `spring-boot-docs/target/contents/reference`
+TIP: The generated documentation is available from `spring-boot-project/spring-boot-docs/target/contents/reference`
@@ -160 +168 @@ appropriate Spring `ApplicationContext`
-* Embedded web applications with a choice of container (Tomcat or Jetty for now)
+* Embedded web applications with a choice of container (Tomcat, Jetty or Undertow)
@@ -172 +180 @@ auto-configuration of the Spring context.
-Auto-configuration attempts to deduce which beans a user might need. For example, If
+Auto-configuration attempts to deduce which beans a user might need. For example, if
@@ -198,2 +206,2 @@ Spring Boot Actuator provides additional auto-configuration to decorate your app
-with features that make it instantly deployable and supportable in production.  For
-instance if you are writing a JSON web service then it will provide a server, security,
+with features that make it instantly deployable and supportable in production. For
+instance, if you are writing a JSON web service then it will provide a server, security,
@@ -201 +209 @@ logging, externalized configuration, management endpoints, an audit abstraction,
-more. If you want to switch off the built in features, or extend or replace them, it
+more. If you want to switch off the built-in features, or extend or replace them, it
@@ -210,2 +218,2 @@ that can be launched using `java -jar`. Generally you will not need to use
-link:spring-boot-tools/spring-boot-gradle-plugin[Gradle] or
-link:spring-boot-tools/spring-boot-maven-plugin[Maven] plugin.
+link:spring-boot-project/spring-boot-tools/spring-boot-gradle-plugin[Gradle] or
+link:spring-boot-project/spring-boot-tools/spring-boot-maven-plugin[Maven] plugin.
@@ -217 +225 @@ Groovy samples for use with the command line application are available in
-link:spring-boot-cli/samples[spring-boot-cli/samples]. To run the CLI samples type
+link:spring-boot-project/spring-boot-cli/samples[spring-boot-cli/samples]. To run the CLI samples type
@@ -226 +234 @@ be built with maven and run by invoking `java -jar target/<sample>.jar`.
-The http://spring.io/[spring.io] site contains several guides that show how to use Spring
+The https://spring.io/[spring.io] site contains several guides that show how to use Spring
@@ -229 +237 @@ Boot step-by-step:
-* http://spring.io/guides/gs/spring-boot/[Building an Application with Spring Boot] is a
+* https://spring.io/guides/gs/spring-boot/[Building an Application with Spring Boot] is a
@@ -232 +240 @@ Boot step-by-step:
-* http://spring.io/guides/gs/actuator-service/[Building a RESTful Web Service with Spring
+* https://spring.io/guides/gs/actuator-service/[Building a RESTful Web Service with Spring
@@ -235 +243 @@ Boot step-by-step:
-* http://spring.io/guides/gs/convert-jar-to-war/[Converting a Spring Boot JAR Application
+* https://spring.io/guides/gs/convert-jar-to-war/[Converting a Spring Boot JAR Application
diff --git a/README.adoc b/README.adoc
index 5acd9ed837..99f9cc7320 100755
--- a/README.adoc
+++ b/README.adoc
@@ -1,2 +1,2 @@
-= Spring Boot image:https://ci.spring.io/api/v1/teams/spring-boot/pipelines/spring-boot/jobs/build/badge["Build Status", link="https://ci.spring.io/teams/spring-boot/pipelines/spring-boot?groups=Build"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
-:docs: https://docs.spring.io/spring-boot/docs/current-SNAPSHOT/reference
+= Spring Boot image:https://build.spring.io/plugins/servlet/buildStatusImage/BOOT-PUB["Build Status", link="https://build.spring.io/browse/BOOT-PUB"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
+:docs: http://docs.spring.io/spring-boot/docs/current-SNAPSHOT/reference
@@ -65,6 +65,4 @@ Having trouble with Spring Boot? We'd like to help!
-  the https://spring.io[spring.io] web-site for a wealth of reference documentation. If
-  you are just starting out with Spring, try one of the https://spring.io/guides[guides].
-* If you are upgrading, read the https://github.com/spring-projects/spring-boot/wiki[release notes]
-  for upgrade instructions and "new and noteworthy" features.
-* Ask a question - we monitor https://stackoverflow.com[stackoverflow.com] for questions
-  tagged with https://stackoverflow.com/tags/spring-boot[`spring-boot`].
+  the http://spring.io[spring.io] web-site for a wealth of reference documentation. If
+  you are just starting out with Spring, try one of the http://spring.io/guides[guides].
+* Ask a question - we monitor http://stackoverflow.com[stackoverflow.com] for questions
+  tagged with http://stackoverflow.com/tags/spring-boot[`spring-boot`].
@@ -98 +96,2 @@ greatest, Spring Boot can be easily built with the
-https://github.com/takari/maven-wrapper[maven wrapper]. You also need JDK 1.8.
+https://github.com/takari/maven-wrapper[maven wrapper]. You also need JDK 1.8 (although
+Boot applications can run on Java 1.6).
@@ -106 +105 @@ If you want to build with the regular `mvn` command, you will need
-https://maven.apache.org/run-maven/index.html[Maven v3.2.1 or above].
+http://maven.apache.org/run-maven/index.html[Maven v3.2.1 or above].
@@ -118 +117,2 @@ https://support.springsource.com/spring_committer_signup[Contributor's Agreement
-before your first change, however trivial._
+before your first change, however trivial. (Or if you filed such an agreement already for
+another project just mention that in your pull request.)_
@@ -122,7 +121,0 @@ before your first change, however trivial._
-First of all, make sure you have built the project:
-
-[indent=0]
-----
-	$ ./mvnw clean install
-----
-
@@ -134 +127 @@ available so you need to build that first since it's not generated by default.
-	$ ./mvnw clean install -pl spring-boot-project/spring-boot-tools/spring-boot-maven-plugin -Pdefault,full
+	$ ./mvnw clean install -pl spring-boot-tools/spring-boot-maven-plugin -Pdefault,full
@@ -137,3 +130,2 @@ available so you need to build that first since it's not generated by default.
-The documentation also includes auto-generated information about the starters. You might
-have that in your local repository already (per the first step) but if you want to refresh
-it:
+The documentation also includes auto-generated information about the starters. To
+allow this information to be collected, the starter projects must be built first:
@@ -143 +135 @@ it:
-	$ ./mvnw clean install -f spring-boot-project/spring-boot-starters
+	$ ./mvnw clean install -f spring-boot-starters
@@ -150 +142 @@ Once this is done, you can build the reference documentation with the command be
-	$ ./mvnw clean prepare-package -pl spring-boot-project/spring-boot-docs -Pdefault,full
+	$ ./mvnw clean prepare-package -pl spring-boot-docs -Pdefault,full
@@ -153 +145 @@ Once this is done, you can build the reference documentation with the command be
-TIP: The generated documentation is available from `spring-boot-project/spring-boot-docs/target/contents/reference`
+TIP: The generated documentation is available from `spring-boot-docs/target/contents/reference`
@@ -168 +160 @@ appropriate Spring `ApplicationContext`
-* Embedded web applications with a choice of container (Tomcat, Jetty or Undertow)
+* Embedded web applications with a choice of container (Tomcat or Jetty for now)
@@ -180 +172 @@ auto-configuration of the Spring context.
-Auto-configuration attempts to deduce which beans a user might need. For example, if
+Auto-configuration attempts to deduce which beans a user might need. For example, If
@@ -206,2 +198,2 @@ Spring Boot Actuator provides additional auto-configuration to decorate your app
-with features that make it instantly deployable and supportable in production. For
-instance, if you are writing a JSON web service then it will provide a server, security,
+with features that make it instantly deployable and supportable in production.  For
+instance if you are writing a JSON web service then it will provide a server, security,
@@ -209 +201 @@ logging, externalized configuration, management endpoints, an audit abstraction,
-more. If you want to switch off the built-in features, or extend or replace them, it
+more. If you want to switch off the built in features, or extend or replace them, it
@@ -234 +226 @@ be built with maven and run by invoking `java -jar target/<sample>.jar`.
-The https://spring.io/[spring.io] site contains several guides that show how to use Spring
+The http://spring.io/[spring.io] site contains several guides that show how to use Spring
@@ -237 +229 @@ Boot step-by-step:
-* https://spring.io/guides/gs/spring-boot/[Building an Application with Spring Boot] is a
+* http://spring.io/guides/gs/spring-boot/[Building an Application with Spring Boot] is a
@@ -240 +232 @@ Boot step-by-step:
-* https://spring.io/guides/gs/actuator-service/[Building a RESTful Web Service with Spring
+* http://spring.io/guides/gs/actuator-service/[Building a RESTful Web Service with Spring
@@ -243 +235 @@ Boot step-by-step:
-* https://spring.io/guides/gs/convert-jar-to-war/[Converting a Spring Boot JAR Application
+* http://spring.io/guides/gs/convert-jar-to-war/[Converting a Spring Boot JAR Application
diff --git a/README.adoc b/README.adoc
index 24b57138b8..5acd9ed837 100755
--- a/README.adoc
+++ b/README.adoc
@@ -2 +2 @@
-:docs: http://docs.spring.io/spring-boot/docs/current-SNAPSHOT/reference
+:docs: https://docs.spring.io/spring-boot/docs/current-SNAPSHOT/reference
@@ -65,2 +65,2 @@ Having trouble with Spring Boot? We'd like to help!
-  the http://spring.io[spring.io] web-site for a wealth of reference documentation. If
-  you are just starting out with Spring, try one of the http://spring.io/guides[guides].
+  the https://spring.io[spring.io] web-site for a wealth of reference documentation. If
+  you are just starting out with Spring, try one of the https://spring.io/guides[guides].
@@ -69,2 +69,2 @@ Having trouble with Spring Boot? We'd like to help!
-* Ask a question - we monitor http://stackoverflow.com[stackoverflow.com] for questions
-  tagged with http://stackoverflow.com/tags/spring-boot[`spring-boot`].
+* Ask a question - we monitor https://stackoverflow.com[stackoverflow.com] for questions
+  tagged with https://stackoverflow.com/tags/spring-boot[`spring-boot`].
@@ -96 +96 @@ You don't need to build from source to use Spring Boot (binaries in
-http://repo.spring.io[repo.spring.io]), but if you want to try out the latest and
+https://repo.spring.io[repo.spring.io]), but if you want to try out the latest and
@@ -106 +106 @@ If you want to build with the regular `mvn` command, you will need
-http://maven.apache.org/run-maven/index.html[Maven v3.2.1 or above].
+https://maven.apache.org/run-maven/index.html[Maven v3.2.1 or above].
@@ -234 +234 @@ be built with maven and run by invoking `java -jar target/<sample>.jar`.
-The http://spring.io/[spring.io] site contains several guides that show how to use Spring
+The https://spring.io/[spring.io] site contains several guides that show how to use Spring
@@ -237 +237 @@ Boot step-by-step:
-* http://spring.io/guides/gs/spring-boot/[Building an Application with Spring Boot] is a
+* https://spring.io/guides/gs/spring-boot/[Building an Application with Spring Boot] is a
@@ -240 +240 @@ Boot step-by-step:
-* http://spring.io/guides/gs/actuator-service/[Building a RESTful Web Service with Spring
+* https://spring.io/guides/gs/actuator-service/[Building a RESTful Web Service with Spring
@@ -243 +243 @@ Boot step-by-step:
-* http://spring.io/guides/gs/convert-jar-to-war/[Converting a Spring Boot JAR Application
+* https://spring.io/guides/gs/convert-jar-to-war/[Converting a Spring Boot JAR Application
diff --git a/README.adoc b/README.adoc
index 1379ada4f5..24b57138b8 100755
--- a/README.adoc
+++ b/README.adoc
@@ -209 +209 @@ logging, externalized configuration, management endpoints, an audit abstraction,
-more. If you want to switch off the built in features, or extend or replace them, it
+more. If you want to switch off the built-in features, or extend or replace them, it
diff --git a/README.adoc b/README.adoc
index 2e237c1aa5..1379ada4f5 100755
--- a/README.adoc
+++ b/README.adoc
@@ -206 +206 @@ Spring Boot Actuator provides additional auto-configuration to decorate your app
-with features that make it instantly deployable and supportable in production.  For
+with features that make it instantly deployable and supportable in production. For
diff --git a/README.adoc b/README.adoc
index 19690adb9f..2e237c1aa5 100755
--- a/README.adoc
+++ b/README.adoc
@@ -207 +207 @@ with features that make it instantly deployable and supportable in production.
-instance if you are writing a JSON web service then it will provide a server, security,
+instance, if you are writing a JSON web service then it will provide a server, security,
diff --git a/README.adoc b/README.adoc
index 57dcfa1cf7..19690adb9f 100755
--- a/README.adoc
+++ b/README.adoc
@@ -134 +134 @@ available so you need to build that first since it's not generated by default.
-	$ ./mvnw clean install -pl spring-boot-tools/spring-boot-maven-plugin -Pdefault,full
+	$ ./mvnw clean install -pl spring-boot-project/spring-boot-tools/spring-boot-maven-plugin -Pdefault,full
@@ -143 +143 @@ it:
-	$ ./mvnw clean install -f spring-boot-starters
+	$ ./mvnw clean install -f spring-boot-project/spring-boot-starters
@@ -150 +150 @@ Once this is done, you can build the reference documentation with the command be
-	$ ./mvnw clean prepare-package -pl spring-boot-docs -Pdefault,full
+	$ ./mvnw clean prepare-package -pl spring-boot-project/spring-boot-docs -Pdefault,full
@@ -153 +153 @@ Once this is done, you can build the reference documentation with the command be
-TIP: The generated documentation is available from `spring-boot-docs/target/contents/reference`
+TIP: The generated documentation is available from `spring-boot-project/spring-boot-docs/target/contents/reference`
diff --git a/README.adoc b/README.adoc
index 5a311bc836..57dcfa1cf7 100755
--- a/README.adoc
+++ b/README.adoc
@@ -1,0 +2,2 @@
+:docs: http://docs.spring.io/spring-boot/docs/current-SNAPSHOT/reference
+
diff --git a/README.adoc b/README.adoc
index e420f5d3ec..5a311bc836 100755
--- a/README.adoc
+++ b/README.adoc
@@ -1,3 +1 @@
-= Spring Boot image:https://build.spring.io/plugins/servlet/buildStatusImage/BOOT-PUB["Build Status", link="https://build.spring.io/browse/BOOT-PUB"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
-:docs: http://docs.spring.io/spring-boot/docs/current-SNAPSHOT/reference
-
+= Spring Boot image:https://ci.spring.io/api/v1/teams/spring-boot/pipelines/spring-boot/jobs/build/badge["Build Status", link="https://ci.spring.io/teams/spring-boot/pipelines/spring-boot?groups=Build"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
diff --git a/README.adoc b/README.adoc
index 3a3e3a40bf..e420f5d3ec 100755
--- a/README.adoc
+++ b/README.adoc
@@ -98,2 +98 @@ greatest, Spring Boot can be easily built with the
-https://github.com/takari/maven-wrapper[maven wrapper]. You also need JDK 1.8 (although
-Boot applications can run on Java 1.6).
+https://github.com/takari/maven-wrapper[maven wrapper]. You also need JDK 1.8.
diff --git a/README.adoc b/README.adoc
index c7f6e9be1b..3a3e3a40bf 100755
--- a/README.adoc
+++ b/README.adoc
@@ -169 +169 @@ appropriate Spring `ApplicationContext`
-* Embedded web applications with a choice of container (Tomcat or Jetty for now)
+* Embedded web applications with a choice of container (Tomcat, Jetty or Undertow)
@@ -181 +181 @@ auto-configuration of the Spring context.
-Auto-configuration attempts to deduce which beans a user might need. For example, If
+Auto-configuration attempts to deduce which beans a user might need. For example, if
diff --git a/README.adoc b/README.adoc
index 58a2ec18a8..c7f6e9be1b 100755
--- a/README.adoc
+++ b/README.adoc
@@ -119,2 +119 @@ https://support.springsource.com/spring_committer_signup[Contributor's Agreement
-before your first change, however trivial. (Or if you filed such an agreement already for
-another project just mention that in your pull request.)_
+before your first change, however trivial._
diff --git a/README.adoc b/README.adoc
index 00582640fd..58a2ec18a8 100755
--- a/README.adoc
+++ b/README.adoc
@@ -123,0 +124,7 @@ another project just mention that in your pull request.)_
+First of all, make sure you have built the project:
+
+[indent=0]
+----
+	$ ./mvnw clean install
+----
+
@@ -132,2 +139,3 @@ available so you need to build that first since it's not generated by default.
-The documentation also includes auto-generated information about the starters. To
-allow this information to be collected, the starter projects must be built first:
+The documentation also includes auto-generated information about the starters. You might
+have that in your local repository already (per the first step) but if you want to refresh
+it:
diff --git a/README.adoc b/README.adoc
index 5ffa82d779..00582640fd 100755
--- a/README.adoc
+++ b/README.adoc
@@ -68 +68 @@ Having trouble with Spring Boot? We'd like to help!
-  for upgrade instructions and ``new and noteworthy'' features.
+  for upgrade instructions and "new and noteworthy" features.
diff --git a/README.adoc b/README.adoc
index cf7ea70640..5ffa82d779 100755
--- a/README.adoc
+++ b/README.adoc
@@ -66,0 +67,2 @@ Having trouble with Spring Boot? We'd like to help!
+* If you are upgrading, read the https://github.com/spring-projects/spring-boot/wiki[release notes]
+  for upgrade instructions and ``new and noteworthy'' features.
diff --git a/README.adoc b/README.adoc
index e387405166..cf7ea70640 100755
--- a/README.adoc
+++ b/README.adoc
@@ -83 +83 @@ requests. If you want to raise an issue, please follow the recommendations below
-* If you need to paste code, or include a stack trace use Markdown ++$$```$$++ escapes
+* If you need to paste code, or include a stack trace use Markdown +++```+++ escapes
diff --git a/README.adoc b/README.adoc
index 581a35e72b..e387405166 100755
--- a/README.adoc
+++ b/README.adoc
@@ -41 +41 @@ Here is a quick teaser of a complete Spring Boot application in Java:
-	@EnableAutoConfiguration
+	@SpringBootApplication
@@ -105 +105 @@ If you want to build with the regular `mvn` command, you will need
-http://maven.apache.org/run-maven/index.html[Maven v3.0.5 or above].
+http://maven.apache.org/run-maven/index.html[Maven v3.2.1 or above].
diff --git a/README.adoc b/README.adoc
index 4e001bf8b3..581a35e72b 100755
--- a/README.adoc
+++ b/README.adoc
@@ -142 +142 @@ Once this is done, you can build the reference documentation with the command be
-	$ ./mvnw clean generate-resources -pl spring-boot-docs -Pdefault,full
+	$ ./mvnw clean prepare-package -pl spring-boot-docs -Pdefault,full
diff --git a/README.adoc b/README.adoc
index 5d3f176ffe..4e001bf8b3 100755
--- a/README.adoc
+++ b/README.adoc
@@ -130 +130 @@ available so you need to build that first since it's not generated by default.
-The documentation also includes auto-generated information about the starter poms. To
+The documentation also includes auto-generated information about the starters. To
diff --git a/README.adoc b/README.adoc
index 7d062173b2..5d3f176ffe 100755
--- a/README.adoc
+++ b/README.adoc
@@ -129,0 +130,8 @@ available so you need to build that first since it's not generated by default.
+The documentation also includes auto-generated information about the starter poms. To
+allow this information to be collected, the starter projects must be built first:
+
+[indent=0]
+----
+	$ ./mvnw clean install -f spring-boot-starters
+----
+
@@ -134 +142 @@ Once this is done, you can build the reference documentation with the command be
-	$ ./mvnw clean install -pl spring-boot-docs -Pdefault,full
+	$ ./mvnw clean generate-resources -pl spring-boot-docs -Pdefault,full
diff --git a/README.adoc b/README.adoc
index 806ff7832b..7d062173b2 100755
--- a/README.adoc
+++ b/README.adoc
@@ -67 +67 @@ Having trouble with Spring Boot? We'd like to help!
-* Ask a questions - we monitor http://stackoverflow.com[stackoverflow.com] for questions
+* Ask a question - we monitor http://stackoverflow.com[stackoverflow.com] for questions
@@ -117 +117 @@ https://support.springsource.com/spring_committer_signup[Contributor's Agreement
-before your first change however trivial. (Or if you filed such an agreement already for
+before your first change, however trivial. (Or if you filed such an agreement already for
@@ -122 +122 @@ another project just mention that in your pull request.)_
-The reference documentation requires the documentation of the maven plugin to be
+The reference documentation requires the documentation of the Maven plugin to be
diff --git a/README.adoc b/README.adoc
index a55065b9f8..806ff7832b 100755
--- a/README.adoc
+++ b/README.adoc
@@ -127 +127 @@ available so you need to build that first since it's not generated by default.
-	$ mvn clean install -pl spring-boot-tools/spring-boot-maven-plugin -Pdefault,full
+	$ ./mvnw clean install -pl spring-boot-tools/spring-boot-maven-plugin -Pdefault,full
@@ -134 +134 @@ Once this is done, you can build the reference documentation with the command be
-	$ mvn clean install -pl spring-boot-docs -Pdefault,full
+	$ ./mvnw clean install -pl spring-boot-docs -Pdefault,full
diff --git a/README.adoc b/README.adoc
index 89f6272f2a..a55065b9f8 100755
--- a/README.adoc
+++ b/README.adoc
@@ -75 +75 @@ Spring Boot uses GitHub's integrated issue tracking system to record bugs and fe
-requests. If you want to raise an issue, please follow the recommendations bellow:
+requests. If you want to raise an issue, please follow the recommendations below:
diff --git a/README.adoc b/README.adoc
index 10cea207a3..89f6272f2a 100755
--- a/README.adoc
+++ b/README.adoc
@@ -114,3 +113,0 @@ is automatically set if you use the maven wrapper.
-NOTE: Make sure that your Git config property "core.autocrlf" setting is set to true
-under Windows to avoid build and test failures due to missing CRLF EOL characters.
-
diff --git a/README.adoc b/README.adoc
index 89f6272f2a..10cea207a3 100755
--- a/README.adoc
+++ b/README.adoc
@@ -113,0 +114,3 @@ is automatically set if you use the maven wrapper.
+NOTE: Make sure that your Git config property "core.autocrlf" setting is set to true
+under Windows to avoid build and test failures due to missing CRLF EOL characters.
+
diff --git a/README.adoc b/README.adoc
index f724818293..89f6272f2a 100755
--- a/README.adoc
+++ b/README.adoc
@@ -94,3 +94,4 @@ You don't need to build from source to use Spring Boot (binaries in
-http://repo.spring.io[repo.spring.io], but if you want to try out the latest and greatest,
-Spring Boot can be http://maven.apache.org/run-maven/index.html[built with maven]
-v3.0.5 or above. You also need JDK 1.8 (although Boot applications can run on Java 1.6).
+http://repo.spring.io[repo.spring.io]), but if you want to try out the latest and
+greatest, Spring Boot can be easily built with the
+https://github.com/takari/maven-wrapper[maven wrapper]. You also need JDK 1.8 (although
+Boot applications can run on Java 1.6).
@@ -100 +101 @@ v3.0.5 or above. You also need JDK 1.8 (although Boot applications can run on Ja
-	$ mvn clean install
+	$ ./mvnw clean install
@@ -102,0 +104,3 @@ v3.0.5 or above. You also need JDK 1.8 (although Boot applications can run on Ja
+If you want to build with the regular `mvn` command, you will need
+http://maven.apache.org/run-maven/index.html[Maven v3.0.5 or above].
+
@@ -107 +111,2 @@ tests there (e.g. in Eclipse go to `Preferences->Java->Installed JREs` and edit
-JRE definition so that all processes are launched with those arguments).
+JRE definition so that all processes are launched with those arguments). This property
+is automatically set if you use the maven wrapper.
diff --git a/README.adoc b/README.adoc
index ae18882654..f724818293 100755
--- a/README.adoc
+++ b/README.adoc
@@ -59 +59 @@ Here is a quick teaser of a complete Spring Boot application in Java:
-Having trouble with Spring Boot, We'd like to help!
+Having trouble with Spring Boot? We'd like to help!
diff --git a/README.adoc b/README.adoc
index 7f7d89e55a..ae18882654 100755
--- a/README.adoc
+++ b/README.adoc
@@ -28 +28 @@ as well as a comprehensive {docs}/htmlsingle/#getting-started-first-application[
-started''] guide. Documentation is published in {docs}/htmlsingle/[HTML],
+started``] guide. Documentation is published in {docs}/htmlsingle/[HTML],
diff --git a/README.adoc b/README.adoc
index 3ce9d92829..7f7d89e55a 100755
--- a/README.adoc
+++ b/README.adoc
@@ -114,0 +115,18 @@ another project just mention that in your pull request.)_
+=== Building reference documentation
+
+The reference documentation requires the documentation of the maven plugin to be
+available so you need to build that first since it's not generated by default.
+
+[indent=0]
+----
+	$ mvn clean install -pl spring-boot-tools/spring-boot-maven-plugin -Pdefault,full
+----
+
+Once this is done, you can build the reference documentation with the command below:
+
+[indent=0]
+----
+	$ mvn clean install -pl spring-boot-docs -Pdefault,full
+----
+
+TIP: The generated documentation is available from `spring-boot-docs/target/contents/reference`
diff --git a/README.adoc b/README.adoc
index 13fc97aa7c..3ce9d92829 100755
--- a/README.adoc
+++ b/README.adoc
@@ -104 +104 @@ NOTE: You may need to increase the amount of memory available to Maven by settin
-a `MAVEN_OPTS` environment variable with the value `-Xmx512m -XX:MaxPermSize=128m`. Remember
+a `MAVEN_OPTS` environment variable with the value `-Xmx512m`. Remember
diff --git a/README.adoc b/README.adoc
old mode 100644
new mode 100755
index 3897ad1116..13fc97aa7c
--- a/README.adoc
+++ b/README.adoc
@@ -96 +96 @@ Spring Boot can be http://maven.apache.org/run-maven/index.html[built with maven
-v3.0.5 or above. You also need JDK 1.7 (although Boot applications can run on Java 1.6).
+v3.0.5 or above. You also need JDK 1.8 (although Boot applications can run on Java 1.6).
diff --git a/README.adoc b/README.adoc
index 241d3b2550..3897ad1116 100644
--- a/README.adoc
+++ b/README.adoc
@@ -64 +64 @@ Having trouble with Spring Boot, We'd like to help!
-* Learn the Spring basics -- Spring Boot is builds on many other Spring projects, check
+* Learn the Spring basics -- Spring Boot builds on many other Spring projects, check
diff --git a/README.adoc b/README.adoc
index a3ccaa68f2..241d3b2550 100644
--- a/README.adoc
+++ b/README.adoc
@@ -1 +1 @@
-= Spring Boot image:https://build.spring.io/plugins/servlet/buildStatusImage/BOOT-PUB["Build Status", link="https://build.spring.io/browse/BOOT-PUB"]
+= Spring Boot image:https://build.spring.io/plugins/servlet/buildStatusImage/BOOT-PUB["Build Status", link="https://build.spring.io/browse/BOOT-PUB"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
diff --git a/README.adoc b/README.adoc
index ff23c0bfac..a3ccaa68f2 100644
--- a/README.adoc
+++ b/README.adoc
@@ -1 +1 @@
-= Spring Boot image:https://build.spring.io/plugins/servlet/buildStatusImage/BOOT-PUB["Build Status", link="https://build.spring.io/browse/BOOT-PUB"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
+= Spring Boot image:https://build.spring.io/plugins/servlet/buildStatusImage/BOOT-PUB["Build Status", link="https://build.spring.io/browse/BOOT-PUB"]
@@ -192,0 +193 @@ be built with maven and run by invoking `java -jar target/<sample>.jar`.
+
@@ -204,0 +206,6 @@ Boot step-by-step:
+
+
+
+== License
+Spring Boot is Open Source software released under the
+http://www.apache.org/licenses/LICENSE-2.0.html[Apache 2.0 license].
diff --git a/README.adoc b/README.adoc
index c6230bfd22..ff23c0bfac 100644
--- a/README.adoc
+++ b/README.adoc
@@ -1 +1 @@
-= Spring Boot image:https://build.spring.io/plugins/servlet/buildStatusImage/BOOT-PUB["Build Status", link="https://build.spring.io/browse/BOOT-PUB"]
+= Spring Boot image:https://build.spring.io/plugins/servlet/buildStatusImage/BOOT-PUB["Build Status", link="https://build.spring.io/browse/BOOT-PUB"] image:https://badges.gitter.im/Join Chat.svg["Chat",link="https://gitter.im/spring-projects/spring-boot?utm_source=badge&utm_medium=badge&utm_campaign=pr-badge&utm_content=badge"]
diff --git a/README.adoc b/README.adoc
index bd710a2808..c6230bfd22 100644
--- a/README.adoc
+++ b/README.adoc
@@ -190,31 +190 @@ Java samples are available in link:spring-boot-samples[spring-boot-samples] and
-be built with maven and run by invoking `java -jar target/<sample>.jar`. The following
-java samples are provided:
-
-* link:spring-boot-samples/spring-boot-sample-simple[spring-boot-sample-simple]
-  -- A simple command line application
-* link:spring-boot-samples/spring-boot-sample-tomcat[spring-boot-sample-tomcat]
-  --  Embedded Tomcat
-* link:spring-boot-samples/spring-boot-sample-jetty[spring-boot-sample-jetty]
-  -- Embedded Jetty
-* link:spring-boot-samples/spring-boot-sample-actuator[spring-boot-sample-actuator]
-  -- Simple REST service with production features
-* link:spring-boot-samples/spring-boot-sample-actuator-ui[spring-boot-sample-actuator-ui]
-  -- A web UI example with production features
-* link:spring-boot-samples/spring-boot-sample-web-ui[spring-boot-sample-web-ui]
-  -- A thymeleaf web application
-* link:spring-boot-samples/spring-boot-sample-web-static[spring-boot-sample-web-static]
-  -- A web application service static files
-* link:spring-boot-samples/spring-boot-sample-batch[spring-boot-sample-batch]
-  -- Define and run a Batch job in a few lines of code
-* link:spring-boot-samples/spring-boot-sample-data-jpa[spring-boot-sample-data-jpa]
-  -- Spring Data JPA + Hibernate + HSQLDB
-* link:spring-boot-samples/spring-boot-sample-integration[spring-boot-sample-integration]
-  -- A spring integration application
-* link:spring-boot-samples/spring-boot-sample-profile[spring-boot-sample-profile]
-  -- example showing Spring's `@profile` support
-* link:spring-boot-samples/spring-boot-sample-traditional[spring-boot-sample-traditional]
-  -- shows more traditional WAR packaging  (but also executable using `java -jar`)
-* link:spring-boot-samples/spring-boot-sample-xml[spring-boot-sample-xml]
-  -- Example show how Spring Boot can be mixed with traditional XML configuration (we
-  generally recommend using Java `@Configuration` whenever possible)
-
+be built with maven and run by invoking `java -jar target/<sample>.jar`.
diff --git a/README.adoc b/README.adoc
index 5a7594a3bd..bd710a2808 100644
--- a/README.adoc
+++ b/README.adoc
@@ -104 +104,4 @@ NOTE: You may need to increase the amount of memory available to Maven by settin
-a `MAVEN_OPTS` environment variable with the value `-Xmx512m -XX:MaxPermSize=128m`
+a `MAVEN_OPTS` environment variable with the value `-Xmx512m -XX:MaxPermSize=128m`. Remember
+to set the corresponding property in your IDE as well if you are building and running
+tests there (e.g. in Eclipse go to `Preferences->Java->Installed JREs` and edit the
+JRE definition so that all processes are launched with those arguments).
diff --git a/README.adoc b/README.adoc
index 47598f012a..5a7594a3bd 100644
--- a/README.adoc
+++ b/README.adoc
@@ -69 +69,20 @@ Having trouble with Spring Boot, We'd like to help!
-* Report bugs with Spring Boot at https://github.com/spring-projects/spring-boot/issues.
+* Report bugs with Spring Boot at https://github.com/spring-projects/spring-boot/issues[github.com/spring-projects/spring-boot/issues].
+
+
+
+== Reporting Issues
+Spring Boot uses GitHub's integrated issue tracking system to record bugs and feature
+requests. If you want to raise an issue, please follow the recommendations bellow:
+
+* Before you log a bug, please https://github.com/spring-projects/spring-boot/search?type=Issues[search the issue tracker]
+  to see if someone has already reported the problem.
+* If the issue doesn't already exist, https://github.com/spring-projects/spring-boot/issues/new[create a new issue].
+* Please provide as much information as possible with the issue report, we like to know
+  the version of Spring Boot that you are using, as well as your Operating System and
+  JVM version.
+* If you need to paste code, or include a stack trace use Markdown ++$$```$$++ escapes
+  before and after your text.
+* If possible try to create a test-case or project that replicates the issue. You can
+  submit sample projects as pull-requests against the
+  https://github.com/spring-projects/spring-boot-issues[spring-boot-issues] GitHub
+  project. Use the issue number for the name of your project.
diff --git a/README.adoc b/README.adoc
index 4a07dcaeb2..47598f012a 100644
--- a/README.adoc
+++ b/README.adoc
@@ -32 +32 @@ formats.
-Here is a quick teaser of a Spring Boot application:
+Here is a quick teaser of a complete Spring Boot application in Java:
@@ -38 +37,0 @@ Here is a quick teaser of a Spring Boot application:
-	import org.springframework.stereotype.*;
@@ -41 +40 @@ Here is a quick teaser of a Spring Boot application:
-	@Controller
+	@RestController
@@ -46 +44,0 @@ Here is a quick teaser of a Spring Boot application:
-		@ResponseBody
diff --git a/README.adoc b/README.adoc
index cea3c0c078..4a07dcaeb2 100644
--- a/README.adoc
+++ b/README.adoc
@@ -79 +79 @@ Spring Boot can be http://maven.apache.org/run-maven/index.html[built with maven
-v3.0.5 or above.
+v3.0.5 or above. You also need JDK 1.7 (although Boot applications can run on Java 1.6).
diff --git a/README.adoc b/README.adoc
index 28aedf780a..cea3c0c078 100644
--- a/README.adoc
+++ b/README.adoc
@@ -1 +1 @@
-= Spring Boot
+= Spring Boot image:https://build.spring.io/plugins/servlet/buildStatusImage/BOOT-PUB["Build Status", link="https://build.spring.io/browse/BOOT-PUB"]
@@ -4,3 +3,0 @@
-^#**Build Status:{nbsp}**#^
-image:https://build.spring.io/plugins/servlet/buildStatusImage/BOOT-PUB["Build Status", link="https://build.spring.io/browse/BOOT-PUB"]
-
diff --git a/README.adoc b/README.adoc
index a7ab8e6e2b..28aedf780a 100644
--- a/README.adoc
+++ b/README.adoc
@@ -1 +1 @@
-= Spring Boot image:https://travis-ci.org/spring-projects/spring-boot.png?branch=master["Build Status", link="https://travis-ci.org/spring-projects/spring-boot"]
+= Spring Boot
@@ -3,0 +4,3 @@
+^#**Build Status:{nbsp}**#^
+image:https://build.spring.io/plugins/servlet/buildStatusImage/BOOT-PUB["Build Status", link="https://build.spring.io/browse/BOOT-PUB"]
+
