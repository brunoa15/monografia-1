diff --git a/README.md b/README.md
index ec522bc8..f3eba038 100644
--- a/README.md
+++ b/README.md
@@ -4 +4 @@ BaasBox
-BaasBox is an Open Source project that aims to provide a general purpose back end for mobile and web apps.
+BaasBox is an Open Source project that aims to provide a backend for mobile and web apps.
@@ -7,0 +8,4 @@ Further information can be found at [www.baasbox.com](http://www.baasbox.com/ "B
+API and SDKs documentation is [here](http://www.baasbox.com/documentation/ "BaasBox Docs").
+
+Community forum is [here](https://groups.google.com/forum/#!forum/baasbox "BaasBox community forum"). 
+
@@ -27 +31,4 @@ The .zip file contains everything you need to run BaasBox.
-Unzip it, set the execution permission on the start.sh file and run it.
+Unzip it, set the execution permission on the start file and run it.
+
+Windows users can use the start.bat file.
+
diff --git a/README.md b/README.md
index 18922d86..ec522bc8 100644
--- a/README.md
+++ b/README.md
@@ -11 +11,3 @@ Build BaasBox
-To build and run BaasBox you need a JDK (not JRE!) (version 8) ([download here](http://www.oracle.com/technetwork/java/javase/downloads/index.html)) and the Play! Framework 2.2.4 ([download here](http://www.playframework.org/download))
+To build and run BaasBox you need a JDK (not JRE!) (version 8) ([download here](http://www.oracle.com/technetwork/java/javase/downloads/index.html)) and the Play! Framework 2.2.4 ([download here](http://www.playframework.org/download)).
+
+*Important*: You must have Play! Framework 2.2.4 installed! BaasBox will not build with other versions.
diff --git a/README.md b/README.md
index 87ecf5c6..18922d86 100644
--- a/README.md
+++ b/README.md
@@ -11 +11 @@ Build BaasBox
-To build BaasBox you need a JDK (not JRE!) (version 6 or above) ([download here](http://www.oracle.com/technetwork/java/javase/downloads/index.html)) and the Play! Framework 2.2.4 ([download here](http://www.playframework.org/download))
+To build and run BaasBox you need a JDK (not JRE!) (version 8) ([download here](http://www.oracle.com/technetwork/java/javase/downloads/index.html)) and the Play! Framework 2.2.4 ([download here](http://www.playframework.org/download))
diff --git a/README.md b/README.md
index 18fb7483..87ecf5c6 100644
--- a/README.md
+++ b/README.md
@@ -35 +35 @@ Default credentials are:
-+ passwrod: admin
++ password: admin
diff --git a/README.md b/README.md
index cddd9d0c..18fb7483 100644
--- a/README.md
+++ b/README.md
@@ -11 +11 @@ Build BaasBox
-To build BaasBox you need a JDK (not JRE!) (version 6 or above) ([download here](http://www.oracle.com/technetwork/java/javase/downloads/index.html)) and the Play! Framework 2.2.3 ([download here](http://www.playframework.org/download))
+To build BaasBox you need a JDK (not JRE!) (version 6 or above) ([download here](http://www.oracle.com/technetwork/java/javase/downloads/index.html)) and the Play! Framework 2.2.4 ([download here](http://www.playframework.org/download))
@@ -35 +35 @@ Default credentials are:
-+ password: admin
++ passwrod: admin
diff --git a/README.md b/README.md
index 11825d02..cddd9d0c 100644
--- a/README.md
+++ b/README.md
@@ -35 +35 @@ Default credentials are:
-+ passwrod: admin
++ password: admin
diff --git a/README.md b/README.md
index e279a600..11825d02 100644
--- a/README.md
+++ b/README.md
@@ -11 +11 @@ Build BaasBox
-To build BaasBox you need a JDK (not JRE!) (version 6 or above) ([download here](http://www.oracle.com/technetwork/java/javase/downloads/index.html)) and the Play! Framework 2.1.5 ([download here](http://www.playframework.org/download))
+To build BaasBox you need a JDK (not JRE!) (version 6 or above) ([download here](http://www.oracle.com/technetwork/java/javase/downloads/index.html)) and the Play! Framework 2.2.3 ([download here](http://www.playframework.org/download))
@@ -20 +20 @@ Go to your local BaasBox source code directory and type:
-`play dist`
+`play clean-all baas`
diff --git a/README.md b/README.md
index a42dd228..e279a600 100644
--- a/README.md
+++ b/README.md
@@ -11 +11 @@ Build BaasBox
-To build BaasBox you need a JDK (not JRE!) (version 6 or above) ([download here](http://www.oracle.com/technetwork/java/javase/downloads/index.html)) and the Play! Framework 2.1.1 ([download here](http://www.playframework.org/download))
+To build BaasBox you need a JDK (not JRE!) (version 6 or above) ([download here](http://www.oracle.com/technetwork/java/javase/downloads/index.html)) and the Play! Framework 2.1.5 ([download here](http://www.playframework.org/download))
diff --git a/README.md b/README.md
index 7968e178..a42dd228 100644
--- a/README.md
+++ b/README.md
@@ -28 +28 @@ BaasBox will create a new database in the ./db directory, and it will start.
-To test it visit <http://www.localhost:9000> and you will see the BaasBox start page.
+To test it visit <http://localhost:9000> and you will see the BaasBox start page.
@@ -30 +30 @@ To test it visit <http://www.localhost:9000> and you will see the BaasBox start
-To access the **Admin Console Panel**, go to <http://www.localhost:9000/console>
+To access the **Admin Console Panel**, go to <http://localhost:9000/console>
diff --git a/README.md b/README.md
index dc0ce211..7968e178 100644
--- a/README.md
+++ b/README.md
@@ -28 +28 @@ BaasBox will create a new database in the ./db directory, and it will start.
-To test it visit <http://localhost:9000> and you will see the BaasBox start page.
+To test it visit <http://www.localhost:9000> and you will see the BaasBox start page.
@@ -30 +30 @@ To test it visit <http://localhost:9000> and you will see the BaasBox start page
-To access the **Admin Console Panel**, go to <http://localhost:9000/console>
+To access the **Admin Console Panel**, go to <http://www.localhost:9000/console>
@@ -39,0 +40 @@ Default credentials are:
+
diff --git a/README.md b/README.md
index 500503b0..dc0ce211 100644
--- a/README.md
+++ b/README.md
@@ -4 +4 @@ BaasBox
-BaasBox is an Open Source project that aims to provide a general purpose server for the backend operations required generally by Mobile Apps and RIAs.
+BaasBox is an Open Source project that aims to provide a general purpose back end for mobile and web apps.
@@ -13 +13 @@ To build BaasBox you need a JDK (not JRE!) (version 6 or above) ([download here]
-Once you will have installed the above software following their installation guides, you will be able to build BaasBox.
+Once you have installed the above software following, you will be able to build BaasBox.
@@ -24,3 +24,3 @@ Play! will build BaasBox and will create a .zip file in the ./dist directory.
-The .zip file contains all that you need to run BaasBox.
-Unzip it, set the execution permission to the start.sh file and run it.
-If all worked properly, BaasBox will create a new database in the ./DB directory, and will start.
+The .zip file contains everything you need to run BaasBox.
+Unzip it, set the execution permission on the start.sh file and run it.
+BaasBox will create a new database in the ./db directory, and it will start.
@@ -28 +28 @@ If all worked properly, BaasBox will create a new database in the ./DB directory
-To test it go to <http://localhost:9000>. The BaasBox should appear.
+To test it visit <http://localhost:9000> and you will see the BaasBox start page.
@@ -30 +30 @@ To test it go to <http://localhost:9000>. The BaasBox should appear.
-To access to the **Admin Console Panel**, goto <http://localhost:9000/console>
+To access the **Admin Console Panel**, go to <http://localhost:9000/console>
@@ -40 +39,0 @@ Default credentials are:
-
diff --git a/README.md b/README.md
index aa59a244..500503b0 100644
--- a/README.md
+++ b/README.md
@@ -28 +28 @@ If all worked properly, BaasBox will create a new database in the ./DB directory
-To test it go to <http://www.localhost:9000>. The BaasBox should appear.
+To test it go to <http://localhost:9000>. The BaasBox should appear.
@@ -30 +30 @@ To test it go to <http://www.localhost:9000>. The BaasBox should appear.
-To access to the **Admin Console Panel**, goto <http://www.localhost:9000/console>
+To access to the **Admin Console Panel**, goto <http://localhost:9000/console>
diff --git a/README.md b/README.md
index 7968e178..aa59a244 100644
--- a/README.md
+++ b/README.md
@@ -4 +4 @@ BaasBox
-BaasBox is an Open Source project that aims to provide a general purpose back end for mobile and web apps.
+BaasBox is an Open Source project that aims to provide a general purpose server for the backend operations required generally by Mobile Apps and RIAs.
@@ -13 +13 @@ To build BaasBox you need a JDK (not JRE!) (version 6 or above) ([download here]
-Once you have installed the above software following, you will be able to build BaasBox.
+Once you will have installed the above software following their installation guides, you will be able to build BaasBox.
@@ -24,3 +24,3 @@ Play! will build BaasBox and will create a .zip file in the ./dist directory.
-The .zip file contains everything you need to run BaasBox.
-Unzip it, set the execution permission on the start.sh file and run it.
-BaasBox will create a new database in the ./db directory, and it will start.
+The .zip file contains all that you need to run BaasBox.
+Unzip it, set the execution permission to the start.sh file and run it.
+If all worked properly, BaasBox will create a new database in the ./DB directory, and will start.
@@ -28 +28 @@ BaasBox will create a new database in the ./db directory, and it will start.
-To test it visit <http://www.localhost:9000> and you will see the BaasBox start page.
+To test it go to <http://www.localhost:9000>. The BaasBox should appear.
@@ -30 +30 @@ To test it visit <http://www.localhost:9000> and you will see the BaasBox start
-To access the **Admin Console Panel**, go to <http://www.localhost:9000/console>
+To access to the **Admin Console Panel**, goto <http://www.localhost:9000/console>
diff --git a/README.md b/README.md
index e161894c..7968e178 100644
--- a/README.md
+++ b/README.md
@@ -4 +4 @@ BaasBox
-BaasBox is an Open Source project that aims to provide a general purpose server for backend operations generally required by Mobile Apps and RIAs.
+BaasBox is an Open Source project that aims to provide a general purpose back end for mobile and web apps.
@@ -13 +13 @@ To build BaasBox you need a JDK (not JRE!) (version 6 or above) ([download here]
-Once you have installed the above software following their installation guides, you will be able to build BaasBox.
+Once you have installed the above software following, you will be able to build BaasBox.
@@ -25,2 +25,2 @@ The .zip file contains everything you need to run BaasBox.
-Unzip it, set the execution permission to the start.sh file and run it.
-If everything worked properly, BaasBox will create a new database in the ./DB directory, and it will start.
+Unzip it, set the execution permission on the start.sh file and run it.
+BaasBox will create a new database in the ./db directory, and it will start.
@@ -28 +28 @@ If everything worked properly, BaasBox will create a new database in the ./DB di
-To test it go to <http://www.localhost:9000>. BaasBox should appear.
+To test it visit <http://www.localhost:9000> and you will see the BaasBox start page.
@@ -30 +30 @@ To test it go to <http://www.localhost:9000>. BaasBox should appear.
-To access to the **Admin Console Panel**, go to <http://www.localhost:9000/console>
+To access the **Admin Console Panel**, go to <http://www.localhost:9000/console>
diff --git a/README.md b/README.md
index 500503b0..e161894c 100644
--- a/README.md
+++ b/README.md
@@ -4 +4 @@ BaasBox
-BaasBox is an Open Source project that aims to provide a general purpose server for the backend operations required generally by Mobile Apps and RIAs.
+BaasBox is an Open Source project that aims to provide a general purpose server for backend operations generally required by Mobile Apps and RIAs.
@@ -13 +13 @@ To build BaasBox you need a JDK (not JRE!) (version 6 or above) ([download here]
-Once you will have installed the above software following their installation guides, you will be able to build BaasBox.
+Once you have installed the above software following their installation guides, you will be able to build BaasBox.
@@ -24 +24 @@ Play! will build BaasBox and will create a .zip file in the ./dist directory.
-The .zip file contains all that you need to run BaasBox.
+The .zip file contains everything you need to run BaasBox.
@@ -26 +26 @@ Unzip it, set the execution permission to the start.sh file and run it.
-If all worked properly, BaasBox will create a new database in the ./DB directory, and will start.
+If everything worked properly, BaasBox will create a new database in the ./DB directory, and it will start.
@@ -28 +28 @@ If all worked properly, BaasBox will create a new database in the ./DB directory
-To test it go to <http://localhost:9000>. The BaasBox should appear.
+To test it go to <http://www.localhost:9000>. BaasBox should appear.
@@ -30 +30 @@ To test it go to <http://localhost:9000>. The BaasBox should appear.
-To access to the **Admin Console Panel**, goto <http://localhost:9000/console>
+To access to the **Admin Console Panel**, go to <http://www.localhost:9000/console>
diff --git a/README.md b/README.md
index aa59a244..500503b0 100644
--- a/README.md
+++ b/README.md
@@ -28 +28 @@ If all worked properly, BaasBox will create a new database in the ./DB directory
-To test it go to <http://www.localhost:9000>. The BaasBox should appear.
+To test it go to <http://localhost:9000>. The BaasBox should appear.
@@ -30 +30 @@ To test it go to <http://www.localhost:9000>. The BaasBox should appear.
-To access to the **Admin Console Panel**, goto <http://www.localhost:9000/console>
+To access to the **Admin Console Panel**, goto <http://localhost:9000/console>
diff --git a/README.md b/README.md
index 537858e8..aa59a244 100644
--- a/README.md
+++ b/README.md
@@ -11 +11 @@ Build BaasBox
-To build BaasBox you need a JDK (not JRE!) (version 6 or above) ([download here](http://www.oracle.com/technetwork/java/javase/downloads/index.html)) and the Play! Framework 2.1.x ([download here](http://www.playframework.org/download))
+To build BaasBox you need a JDK (not JRE!) (version 6 or above) ([download here](http://www.oracle.com/technetwork/java/javase/downloads/index.html)) and the Play! Framework 2.1.1 ([download here](http://www.playframework.org/download))
diff --git a/README.md b/README.md
index b8a2316b..537858e8 100644
--- a/README.md
+++ b/README.md
@@ -36,0 +37,4 @@ Default credentials are:
+
+
+[![Bitdeli Badge](https://d2weczhvl823v0.cloudfront.net/baasbox/baasbox/trend.png)](https://bitdeli.com/free "Bitdeli Badge")
+
diff --git a/README.md b/README.md
index a90385cd..b8a2316b 100644
--- a/README.md
+++ b/README.md
@@ -11 +11 @@ Build BaasBox
-To build BaasBox you need a JDK (not JRE!) (version 6 or above) ([download here](http://www.oracle.com/technetwork/java/javase/downloads/index.html)) and the Play! Framework 2.0.4 ([download here](http://www.playframework.org/download))
+To build BaasBox you need a JDK (not JRE!) (version 6 or above) ([download here](http://www.oracle.com/technetwork/java/javase/downloads/index.html)) and the Play! Framework 2.1.x ([download here](http://www.playframework.org/download))
diff --git a/README.md b/README.md
index 8abe6580..a90385cd 100644
--- a/README.md
+++ b/README.md
@@ -1 +1 @@
-baasbox
+BaasBox
@@ -4 +4,33 @@ baasbox
-The BaasBox server
\ No newline at end of file
+BaasBox is an Open Source project that aims to provide a general purpose server for the backend operations required generally by Mobile Apps and RIAs.
+
+Further information can be found at [www.baasbox.com](http://www.baasbox.com/ "BaasBox site").
+
+Build BaasBox
+-------------
+### Prerequisites
+To build BaasBox you need a JDK (not JRE!) (version 6 or above) ([download here](http://www.oracle.com/technetwork/java/javase/downloads/index.html)) and the Play! Framework 2.0.4 ([download here](http://www.playframework.org/download))
+
+Once you will have installed the above software following their installation guides, you will be able to build BaasBox.
+
+Download the source code from [GitHub](https://github.com/baasbox/baasbox)
+
+### Build
+Go to your local BaasBox source code directory and type:
+
+`play dist`
+
+Play! will build BaasBox and will create a .zip file in the ./dist directory.
+
+The .zip file contains all that you need to run BaasBox.
+Unzip it, set the execution permission to the start.sh file and run it.
+If all worked properly, BaasBox will create a new database in the ./DB directory, and will start.
+
+To test it go to <http://www.localhost:9000>. The BaasBox should appear.
+
+To access to the **Admin Console Panel**, goto <http://www.localhost:9000/console>
+
+Default credentials are:
+
++ username: admin
++ passwrod: admin
++ application code: 1234567890
